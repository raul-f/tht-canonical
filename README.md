# Take Home Test from Canonical

Built using the WordPress API and Vanilla Framework.

To run it, clone the repository, make sure you have Node.js installed, and then run the following commands:

```bash
npm install
npm run dev
```

